'use strict';

module.exports = function () {
    return {
        options: {
            install: true,
            verbose: true,
            copy: false,
            cleanBowerDir: false
        },
        install: {
        }
    };
};
